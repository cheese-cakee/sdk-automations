{
    "name": "@hiero-hackers/automation-executor",
    "version": "0.0.0",
    "private": true,
    "description": "The recovery-loop executor decided by protocol 6.5 (design/operations/storage-decision.md): journal detects, GitHub resolves, idempotency class decides the retry. Candidate implementation pending stage-four ratification. Built alongside its crash-grid harness.",
    "type": "module",
    "engines": {
        "node": ">=23.4"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@hiero-hackers/automation-core": "workspace:*",
        "@hiero-hackers/automation-store": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.6.0",
        "vitest": "^4.0.0"
    }
}
