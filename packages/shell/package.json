{
    "name": "@hiero-hackers/automation-shell",
    "version": "0.0.0",
    "private": true,
    "description": "Authenticated GitHub webhook intake, durable linked-issue processing, and canonical dry-run reports.",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:watch": "vitest",
        "start": "tsx src/main.ts"
    },
    "dependencies": {
        "@hiero-hackers/automation-core": "workspace:*",
        "@hiero-hackers/automation-store": "workspace:*"
    },
    "devDependencies": {
        "@stryker-mutator/core": "^9.6.1",
        "@stryker-mutator/vitest-runner": "^9.6.1",
        "@types/node": "^24.0.0",
        "tsx": "^4.23.12",
        "typescript": "^5.6.0",
        "vitest": "^4.0.0"
    }
}
