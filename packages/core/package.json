{
    "name": "@hiero-hackers/automation-core",
    "version": "0.0.0",
    "private": true,
    "description": "Strict linked-issue configuration and pure advisory decisions.",
    "type": "module",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "engines": {
        "node": ">=24"
    },
    "scripts": {
        "test": "tsc --noEmit && vitest run",
        "test:watch": "vitest",
        "test:coverage": "tsc --noEmit && vitest run --coverage --testTimeout=15000"
    },
    "devDependencies": {
        "@stryker-mutator/core": "^9.6.1",
        "@stryker-mutator/vitest-runner": "^9.6.1",
        "@types/node": "^24.0.0",
        "fast-check": "^4.9.0",
        "typescript": "^5.6.0",
        "vitest": "^4.0.0"
    },
    "dependencies": {
        "yaml": "^2.9.0"
    }
}
