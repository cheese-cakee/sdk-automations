# Decision and Evidence Register

> This register separates principles that current evidence supports from hypotheses that still need
> maintainer review or sandbox experiments. A document does not become approved merely because it appears in
> this repository or in pull request 23.

## 1. Status meanings

The register uses the following status values.

| Status | Meaning |
|---|---|
| `supported` | The audits, accepted proposal, and current research are strong enough to guide the next work. |
| `hypothesis` | The idea is technically plausible, but maintainers or experiments must still confirm it. |
| `reopened` | New evidence has challenged an earlier proposal, so it must not guide implementation yet. |
| `ratified` | Maintainers have explicitly approved the decision and recorded the date and evidence. |
| `replaced` | A later decision has replaced the earlier proposal. |

## 2. Supported product principles

| Identifier | Principle | Evidence |
|---|---|---|
| P1 | The product is one hosted GitHub App with repository-selected capabilities. | The accepted proposal and `design/planning/goals.md` support this direction. |
| P2 | A repository must explicitly enable every workflow-changing capability, and every user-facing capability defaults to off. | Maintainer consent and the different C++, Python, and JavaScript workflows require explicit opt-in. |
| P3 | A capability does not call or import another capability. | The coupling audit shows that direct and hidden sibling contracts are difficult to test and change. |
| P4 | A capability receives only its own configuration and a narrow platform interface. | The C++ shared-config coupling and GitHub API mock drift support a smaller boundary. |
| P5 | GitHub remains authoritative for visible repository facts. | Labels, assignees, reviews, comments, and open or closed state must continue to reflect human edits. |
| P6 | The design does not assume that GitHub alone stores every operational recovery record. | Webhook delivery, multi-call recovery, and coordination require feasibility experiments. |
| P7 | Repository labels and fields are mappings or profile defaults rather than universal platform strings. | The three audited SDKs use materially different workflow models. |
| P8 | Testing begins with a development App and personal sandbox before any Hiero repository receives writes. | The permission and failure risks require an isolated validation path. |
| P9 | A production webhook receiver durably accepts work before it acknowledges the delivery. | GitHub requires a quick response and does not automatically redeliver every failed delivery, so an acknowledged in-memory event cannot provide reliable recovery. Demonstrated in the sandbox (experiment 6.2, 2026-07-23): a crash after acknowledgement but before durable acceptance lost the delivery while GitHub's ledger recorded success; an ack-first relay in the transport path recreates the same loss window with no crash at all. |

## 3. Earlier design proposals and their current status

The identifiers below preserve references from the existing documents. Their current status is explicit so
that older text cannot be mistaken for approval.

| Identifier | Earlier proposal | Current status | Required next evidence |
|---|---|---|---|
| D1 | GitHub and App-authored comments provide all durable state, so the App needs no owned operational store. | `replaced` | Rejected by experiment 6.5 (2026-07-23): deduplication, pending-effect detection, and coordination each required owned state, and GitHub's delivery ledger records success for deliveries the receiver lost (6.2). Replaced by the storage decision (`design/operations/storage-decision.md`): a small single-file owned store, with GitHub keeping all effect outcomes. Ratification pending per §5. |
| D2 | Issues and pull requests use separate workflow models, and cross-entity writes require an explicit operation. | `hypothesis` | Candidate capabilities must show which cross-entity reads and writes they need. |
| D3 | Every repository uses one fixed twelve-label taxonomy. | `replaced` | P7 replaces this proposal with stable internal meanings and repository mappings or optional profiles. |
| D4 | The platform removes only named managed labels and never removes a namespace prefix. | `supported` | Adapter tests must enforce the rule for every label operation. |
| D5 | The full Hiero workflow profile has one invariant for each position. | `hypothesis` | Repositories that want the profile must review the invariants before activation. |
| D6 | Human edits and capability requests use different rulebooks. | `hypothesis` | Concurrency and manual-edit scenarios must confirm the behavior for each selected capability. |
| D7 | The App does not reverse a newer human change unless maintainers approve a named exception. | `supported` | Effect tests must prove the precondition and newer-change checks. |
| D8 | Incoherent mapped states use the five classes described in `core/manual-edits.md`. | `replaced` | The five-class scheme no longer exists: `core/manual-edits.md` §3 was rewritten to a deliberately smaller first policy — no automatic repair, no guessing, a conflict verdict that changes nothing — and `core/src/workflow/project.ts` encodes it (D35–D37). Detailed conflict categories return only if a selected capability demonstrates the need. This row cited document text that had silently drifted away; caught 2026-07-25. |
| D9 | Timeline timestamps can enforce the newer-fact rule at acceptable cost. | `supported` | Cost confirmed by experiment 6.4 (2026-07-23): a busy issue's full timeline is one request (up to one hundred events, about half a second), conditional re-reads cost nothing, and a fifty-repository sweep fits the measured budget. Timestamp reliability for the newer-fact rule still rides with the D6/D8 manual-edit scenarios. |
| D10 | Clock-triggered destructive actions always warn before they act and provide a simple reversal. | `supported` | Each destructive capability must provide its own warning, grace, cancellation, and rollback tests. |
| D11 | Removing `blocked` resets an inactivity clock instead of resuming it. | `hypothesis` | Maintainers must decide this policy for a profile that enables inactivity handling. |
| D12 | Capabilities provide structured comment content, while the platform owns managed comment identity and writes. | `supported` | The managed-comment sandbox test must prove idempotent update and authorship checks. |
| D13 | Exactly two comment types may carry metadata that the platform reads back. | `supported` | Narrowed by experiment 6.5 (2026-07-23): metadata is read back as effect identity and receipt — the marker is what made retry-after-check safe — but it is not operational storage and records nothing before a write lands. See `design/operations/storage-decision.md`. |
| D14 | Resolved managed comments are shortened instead of deleted. | `hypothesis` | Maintainers must review the contributor experience for the first real comment type. |
| D15 | GitHub closing references are the only supported issue and pull request link mechanism. | `hypothesis` | The first capability that needs links must confirm the repository policy and fork behavior. |
| D16 | Skill credit is organization-wide while each repository chooses issue difficulty. | `reopened` | A skill ladder is optional, and repositories that request it must decide credit scope and completion rules. |
| D17 | Configuration exposes only choices on which reasonable repositories differ. | `supported` | Each key must state the repository choice that justifies it and the safe validation range. |
| D18 | One process and business-hours operation are sufficient because later sweeps repair missed work. | `supported` | Experiments 6.2 and 6.5 (2026-07-23): a single durable-first process plus the owned journal and a reconciliation sweep over the deliveries API (thirty-day retention) recovered every induced loss, including deliveries GitHub records as successfully delivered. Two conditions attach: detection requires the owned store (the ledger never flags a loss), and availability beyond business hours remains an operator decision under Q1/Q13. |
| D19 | A hosted App is preferable to copied repository workflows. | `supported` | The project still needs an operator and hosting decision before production deployment. |
| D20 | One adapter controls rate limits, retries, pacing, and conditional reads. | `supported` | Measured by experiment 6.4 (2026-07-23): uniform one-unit pricing, free 304 re-reads, and a content-creation secondary limit that arrives with no `retry-after` header near eighty writes per minute. One consequence is now explicit: the adapter must own retry and backoff itself — Octokit's default plugins absorb exactly these signals invisibly. |
| D21 | Every failure class has one primary audience and a clear recovery message. | `supported` | The first implementation must verify the channel and permission needed for each message. |
| D22 | Rollout uses a personal sandbox, a Hiero Hackers sandbox, shadow mode, reversible writes, and explicit kill switches. | `supported` | Owners and entry criteria must be recorded before each environment is used. |
| D23 | A capability declares its configuration, inputs, intents, permissions, triggers, and operational needs. | `hypothesis` | The first two candidate capabilities must fit the contract without receiving a raw GitHub client. Experiment 6.3 (`FINDING(config-capability-registry-gap)`) adds a requirement: the platform needs a capability registry checked at configuration load, or configurations enabling unknown capabilities pass validation silently. Experiment 6.5 adds another: each declared effect should name its idempotency class, since a lost-response retry duplicates comment creation but is harmless for label addition. |
| D24 | Current-state checks and comment records can recover every interrupted multi-call operation. | `replaced` | Rejected by experiment 6.5 (2026-07-23): current state cannot see pending work (an absent effect is indistinguishable from one never requested), and after a lost response a naive retry duplicated the effect on the first attempt. Replaced by the storage decision's model — the intent journal detects, a GitHub re-read resolves. Ratification pending per §5. |
| D25 | The threat controls in `operations/threat-model.md` are sufficient for the first release. | `hypothesis` | A red-team pass and sandbox evidence must confirm the controls before a real pilot. |
| D26 | The twelve GitHub label strings are fixed in code and cannot be configured. | `replaced` | P7 replaces this proposal. Code may keep stable meanings, while repositories configure mappings or select a profile. |
| D27 | Comment metadata is the App's write-ahead log and only durable operational state. | `replaced` | Rejected by experiment 6.5 (2026-07-23) on observed grounds: it records nothing before a write lands, covers only comment-shaped effects, and cannot coordinate — the two-worker race duplicated the effect even with the read-check this proposal relies on, because GitHub offers no conditional create. Each write-ahead write would also cost into the unsignaled secondary limit measured in 6.4. Replaced by the storage decision. Ratification pending per §5. |

### Hypotheses surfaced by the pure-logic implementation

Coding the design prose into the implementation packages (`core/`, `store/`, `executor/`) forced
ambiguities into concrete choices. Each choice is tagged `FINDING(...)` in the source at the exact place the assumption was made, and
the invariant tests in `core/test/` exercise the chosen behavior. The code is evidence that the choice is
*coherent*, not that it is *right* — each row still needs the named review.

| Identifier | Choice the code had to make | Current status | Required next evidence |
|---|---|---|---|
| D28 | `blocked` is an orthogonal pause flag, not a workflow position: an item keeps its position while blocked, and unblocking restores it unchanged. `taxonomy.md` §2 lists `blocked` as a meaning, but neither state diagram contains it, and `safety.md` treats it as a pause. (`core/src/workflow/meanings.ts`, `FINDING(taxonomy-blocked)`.) | `hypothesis` | Maintainers must confirm the flag model when reviewing the workflow profile, or the diagrams must gain explicit `blocked` positions. |
| D29 | Manual entry into any state is recorded as observed reality to reconcile, not as a requestable transition: the exhaustive transition matrix rejects module requests for edges the diagrams do not contain, while human edits may still land anywhere. The prose rule "every state has a non-module way in" implies edges the diagrams omit. (`core/src/workflow/apply.ts`, `FINDING(taxonomy-manual-entry)`.) | `supported` | The manual-edit scenarios under D6/D8 must confirm reconciliation matches maintainer expectations for each profile state. |
| D30 | Grace periods have a floor of one day (`MIN_GRACE_DAYS = 1`): a destructive clock-triggered action cannot be configured to warn and act in the same instant. `safety.md` §4 requires "safe" bounds but names none. (`core/src/safety/destructive.ts`, `FINDING(safety-grace-floor)`.) | `hypothesis` | Maintainers must ratify the floor value (or a different one) when the first destructive capability is reviewed under D10. |
| D31 | The no-configuration mode is named `observe`: with no reviewed configuration, the App reads and reports but performs no workflow-changing writes. `schema.md` §2.2 requires the behavior but does not name the mode; `observe` was chosen over `disabled` because the platform still watches and audits. (`core/src/config/schema.ts`, `FINDING(config-no-config-mode)`.) | `hypothesis` | The configuration review under Q14 must confirm the mode name. That fail-closed validation always lands in `observe` is now demonstrated: experiment 6.3 (2026-07-23) drove every invalid fetched configuration — bad value, unknown key, wrong schema version — to zero configuration and `observe`, identical to the unit fixtures. |
| D32 | The stage-three feasibility experiments (build plan §6.1–§6.6) are capability-independent and may run while the stage-two capability-ranking gate is still open. None of the six protocols references a specific capability; the ranking is consumed by stage four's review, not by the experiments. The experiment protocols (local evidence records under `experiments/`, untracked by design) are the evidence that no step depends on the ranking. | `supported` | If any protocol step turns out to need a capability choice mid-run, this row is `reopened` and that step waits for the stage-two gate. |
| D33 | The rule-5 comparison lives in the core, and an exact-timestamp tie goes to the human: `evaluateWrite` refuses when the newest human change on the touched state is at or after the request's `causeObservedAt` (`>=`, not `>`), with the shell required to exclude the causing event itself when supplying `latestHumanChangeAt` — a human edit that both triggers the request and is the newest touch is the cause, not a conflict. GitHub timestamps have second granularity, so exact ties are plausible. (`core/src/safety/write.ts`, `FINDING(safety-human-tie)`.) | `supported` | Maintainers must confirm the tie-break and the causing-event exclusion when the first capability's executor tests are reviewed under D7. |
| D34 | Label mappings are fully injective: `parseConfig` rejects any two meanings mapped to the same label string. `schema.md` §3 forbids "two incompatible meanings" sharing a label but never defines *incompatible*; full injectivity is the strictest defensible reading, and the observation projection (D35) depends on label→meaning being unambiguous. A same-entity-only rule (`ready` and `needsReview` sharing one "attention" label, disambiguated by entity) would be a deliberate relaxation. (`core/src/config/validate.ts`, `FINDING(config-label-injectivity)`.) | `hypothesis` | The configuration review under Q14 must define "incompatible" — confirm full injectivity or ratify the same-entity relaxation. |
| D35 | The observation projection's three readings, encoded together in `core/src/workflow/project.ts`: a position meaning of the other entity's flow observed on an item is left alone and reported as ignored, never a conflict (`FINDING(observe-cross-entity)` — `manual-edits.md` §3 rule 1's "unrelated labels" read to cover the other flow); `blocked` with no position is a legal observation projecting to "no position, paused" (`FINDING(observe-blocked-alone)` — D28's orthogonal flag applied at the observation boundary); and a closed item keeps whatever position labels it carries, unrepaired, with the state machine's closed guard refusing transitions regardless (`FINDING(observe-closed-position)` — silence on closure per `manual-edits.md` §6). | `hypothesis` | The D6/D8 manual-edit scenario review confirms all three readings together; the blocked reading ratifies or falls with D28. |
| D36 | Folded into D35 (2026-07-25): `blocked` with no position label. | `replaced` | See D35. |
| D37 | Folded into D35 (2026-07-25): closed items keep their position labels. | `replaced` | See D35. |
| D38 | Fail-closed validation is whole-file: one error anywhere yields no configuration, dropping the repository to `observe` and stopping every working capability until the file is fixed. Last-known-good was rejected because an older automation decision must not outlive a newer human edit (a maintainer's attempt to disable a behavior would fail while the behavior keeps running); partial salvage was rejected because an uninterpretable fragment can change the meaning of valid-looking fragments (the reason §2.7 rejects unknown keys) and a half-running state is the hardest to debug. The loud full stop is acceptable maintainer experience only with two shell-owned mitigations: the configuration report (`projections.md` §3) within seconds of the breaking edit, and PR-time validation as a check run on any PR touching the file, so invalid configuration normally never reaches the default branch. (`core/src/config/parse.ts`, `FINDING(config-fail-closed-granularity)`.) | `hypothesis` | Maintainers must confirm the full-stop-with-report experience during the ring-one pilot (Q8), and the PR-time validation check must exist before any repository runs `active`. |
| D39 | An active kill switch refuses even pure observations: the kill-switch check precedes the observation short-circuit in `evaluateWrite`, so "stop" means stop reading-and-recording, not merely stop writing. `safety.md` §5 does not say which reading wins; the total stop is encoded because a kill switch is an operator's emergency brake, and residual activity after pulling it costs trust exactly when trust is lowest. The rest of the check order carries no outcome semantics — it only decides which machine-readable `code` is reported when several rules fail at once, and the verdict-code tests freeze that reporting order as contract. (`core/src/safety/write.ts`, `FINDING(safety-killswitch-observations)`.) | `hypothesis` | The kill-switch design under D22 must confirm the total-stop semantics — in particular whether operator alerts and security controls (which `manual-edits.md` §5 exempts from an item-level block) are also exempt from a kill switch, which this code does not model. |
| D40 | Body-regex failure classification is snapshot evidence, not contract: the rules in `core/src/github/failures.ts` match prose GitHub sent on 2026-07-23, and GitHub may reword it at any time. The mitigation is structural plus operational: an unrecognized 403 degrades into the explicit `forbiddenUnrecognized` class carrying the body verbatim (never a fabricated diagnosis), and a periodic sandbox re-probe must re-validate the observed fixtures — a rise in `forbiddenUnrecognized` operator reports is the signal that the catalogue needs a new observation run. (`core/src/github/failures.ts`, `FINDING(failures-prose-snapshot)`.) (`FINDING(secondary-limit-no-wait-signal)` — the 403 secondary limit carries no `retry-after` header, so the wait is inferred from body prose and a documented one-minute floor.) | `supported` | The operations review under Q1/Q13 must schedule the re-probe cadence and name the owner of fixture refresh. |
| D41 | Effect claims are leases, not permanent locks: `claim` atomically takes over a claim whose timestamp is at or before the caller's stale threshold. The earlier claim that journal plus read-back makes live takeover safe was falsified by the 2026-07-28 risk review: a still-live worker can resume from a pre-takeover snapshot or have a non-idempotent request in flight while its successor sees absence and resends. SQLite ownership checks cannot cancel a remote GitHub request. (`store/src/store.ts`; `executor/src/recovery.ts`; `FINDING(store-claim-lease)`.) | `reopened` | Stage four must choose and test an explicit overlap contract: renewal and adapter deadlines shorter than the lease, takeover delayed beyond the maximum in-flight lifetime, and an operator stop rather than resend when absence cannot be established safely. A deterministic overlapping-worker oracle is required. |
| D42 | The effect journal carries a durable per-call `attempt` counter, and a `done` row is immutable to `intent`: re-declaring a still-open call increments the counter in the same single-statement upsert, while acknowledged history can never regress to `sent` (the original `INSERT OR REPLACE` could silently un-happen a receipt, sending recovery to re-resolve completed work). This amends the ratification-pending four-table schema by one column, implementing the storage decision's own "retries with bounded history" cell — which the decided schema claimed but did not support — and making retry bounds (core's `MAX_RATE_LIMIT_ATTEMPTS`) survive restart instead of resetting with the process. A count, not per-attempt rows: forensic history was not demanded by any 6.5 recovery. Pre-ratification store files are not migrated. (`store/src/store.ts`, `FINDING(store-journal-attempts)`.) | `supported` | The stage-four storage ratification reviews the amended `effect_journal` schema alongside the original four-table decision. |
| D43 | The reconciliation sweep the design leans on (D18) has a concrete store API: `schedule` gains a `claimed_at` column stamped by `claimDue`, `requeueStuck(claimedBefore)` atomically returns stuck `running` rows to `pending` (stuckness judged by claim age, never due time, so a backlog catch-up is not stolen from; requeued work re-fires through the normal `claimDue` path — no parallel firing mechanism), and `openIntents(before)` lists every unresolved `sent` journal row as the recovery loop's worklist. A slow-but-alive schedule handler can be requeued and fire twice — accepted on D41's grounds: the journal plus GitHub re-read is the duplicate-prevention layer. The requeue threshold is the sweep's operations decision, as is retention: `seen_delivery`, `done` journal rows, and released-claim leftovers grow without bound until the sweep design names pruning windows (the deliveries API's own retention is thirty days; the store's must be decided, not defaulted). (`store/src/store.ts`, `FINDING(store-sweep-api)`.) | `supported` | The stage-four storage ratification reviews the amended `schedule` schema, and the sweep design under D18 sets the requeue threshold, retention windows, and D41's lease duration together. |
| D44 | A call re-sent `MAX_CALL_ATTEMPTS = 5` times without a confirmed outcome stops retrying and surfaces to the operator as unresolved. The storage decision requires "retries with bounded history" but names no bound — the same unnamed-limit pattern as the grace-period floor (D30). The bound reads the journal's durable attempt counter (D42), so it survives restarts; the crash grid demonstrates convergence well inside it. (`executor/src/recovery.ts`, `FINDING(executor-attempt-bound)`.) | `supported` | The stage-four storage ratification confirms the bound value alongside D42's counter. |
| D45 | An open journal row that no longer matches the current plan (seq beyond the plan, or a different intent at that seq) surfaces as unresolved and is never remapped: `manual-edits.md` §9 rules that intents created under an old configuration revision are invalid, so the executor stops rather than guess a correspondence between old and new plans. (`executor/src/recovery.ts`, `FINDING(executor-stale-plan)`.) | `supported` | The configuration-revision design under Q14 must define how surfaced stale effects are closed out (operator action, automatic abandon after review, or re-issue under the new revision). |
| D46 | The recovery loop's exactly-once guarantee is proven only relative to a CONSISTENT read-back: the crash grid's fake world answers `readBack` with perfect read-after-write consistency, but real GitHub reads can lag writes, and a stale "absent" immediately after a landed create would make the loop duplicate despite following every rule. The loop is correct given the precondition; the precondition is not free. The real port owes a confirmed-fresh read (or a bounded delay and re-read) before answering "absent". (`executor/src/recovery.ts`, `FINDING(executor-readback-consistency)`; the harness declares where its world is kinder than GitHub.) | `supported` | Measured by protocol 6.7 (2026-07-25, `design/operations/read-after-write.md`): 40/40 trials — comment→list and label→read — visible on the FIRST immediate read; no staleness observed. The freshness rule is encoded (`executor/src/policy.ts`): "present" on first sight, "absent" only after two reads ≥1 s apart (~2× observed p95), because a wrong "absent" duplicates a non-idempotent call. Remaining for stage four: the port implements the rule; re-measure if the resolver ever uses GraphQL or search reads (unmeasured; search indexing is known to lag). |
| D47 | Closure is recorded as a REASON on the work-item state, not as a position: `closedBy` is `merged`, `closedByHuman`, or `completedByLinkedMerge`, `null` while open, read from GitHub's own fields (`merged_at`, `state_reason`) and never written as a label. `taxonomy.md` §5 wrote "The pull request closes or merges" as one edge, discarding the distinction at exactly the point downstream policy needs it: progression credits a merged linked pull request and not an abandoned one (`design/modules/progression.md`), and the audited C++ post-merge cleanup is gated `merged == true` (`design/audit/services-cpp.md`). Modelled orthogonally for the same reason as `blocked` (D28) — as a meaning it would become mappable, and a merged pull request still carrying `status: needs review` would project as a two-position conflict, silencing observation on the items most worth reporting. (`core/src/workflow/meanings.ts`, `core/src/workflow/project.ts`, `FINDING(taxonomy-closure-reason)`.) | `hypothesis` | The D6/D8 manual-edit scenario review confirms the reason set; the first capability that consumes closure (progression or post-merge cleanup) proves the three values are sufficient and that no fourth is needed for an automation-initiated close. |
| D48 | Three corrections to the pull-request flow, all found by reading the tables against the audit rather than against the prose. (1) `readyToMerge → needsRevision` was MISSING: an approved pull request whose checks broke had no path out except one asserting new commits, yet the audited Sibling Conflict Re-check re-reads every open pull request's `mergeable` state when a *different* pull request merges and swaps its status label (`design/audit/services-cpp.md`) — checks break with no push. (2) `needsReview → needsRevision` accepted only `checksFailed`, though §5 labels that edge "New evidence requires contributor action" and the audited PR Review Label Applicator performs it on a `changes_requested` review; `reviewRequestedChanges` is added, and observing it requires the `pull_request_review` subscription experiment 6.6 found absent. (3) `newCommitsInvalidatedApproval` is renamed `approvalInvalidated` — the old name bundled a trigger with its consequence and could not express a dismissed review or a changed base. The first implementation's narrowing in (2) was made silently, with no `FINDING` tag, which is why it never reached this register. (`core/src/workflow/transitions.ts`, `FINDING(taxonomy-approved-checks-broke)`, `FINDING(taxonomy-review-cause)`, `FINDING(taxonomy-approval-cause)`.) | `hypothesis` | Maintainer confirmation that the audited C++ behaviors belong in the profile, and the `pull_request_review` subscription decision under the permission ceiling. All three assume `readyToMerge` is a STORED position; if the open §10 question resolves to "derived", these edges collapse into that derivation and the corrections become moot rather than wrong. |
| D49 | Reopening is a closure CLEAR, not a transition, and it lives outside the edge tables: closing never removes the position labels (D35), so a reopened item returns exactly where it was and no position moves — the same shape as `blocked` (D28), and the reason the first implementation's "except reopen (not modelled yet)" comment could not be resolved inside the tables. One invariant falls out of D47's reason: a **merged pull request can never reopen**, which GitHub enforces and `applyReopen` refuses as `mergedNotReopenable` rather than omitting. Reopening an already-open item is refused (`notClosed`) rather than absorbed as a silent no-op. (`core/src/workflow/apply.ts`, `FINDING(taxonomy-reopen)`.) | `hypothesis` | The D6/D8 manual-edit scenario review confirms that a reopened item should resume its old position rather than re-enter triage — the alternative reading, and the one a repository whose triage is the entry gate might prefer. |
| D50 | Two mechanical hardenings, neither carrying taste. (1) Transition causes are scoped per entity (`IssueCause`, `PrCause`): the first implementation shared one flat union, so `triageCompleted` on a pull request type-checked and was refused only at runtime — the same "make misuse unrepresentable" rule `ids.ts` applies to delivery ids, and no cause is legal in both flows except `humanClosed`. (2) The doc-drift detector `core/README.md` promised now exists: `checks/test/doc-drift.test.ts` parses the state diagrams out of `taxonomy.md` and asserts they match the tables edge for edge, in both directions. It compares (from, to) pairs only — arrow prose stays human-written and causes stay covered by the exhaustive matrix — so a cause added without a doc edit still slips through, but a whole edge no longer can. This is the gap that let D48's missing edge sit in both artifacts undetected, and the same class of gap as the D8 drift caught on 2026-07-25. (`FINDING(taxonomy-entity-scoped-causes)`.) | `supported` | None required for the mechanism itself. The remaining drift surface is named above: causes, and the register's own prose. Generating the diagrams from the tables outright would close it and stays the cheaper long-term option. |
| D51 | Ordering evidence for rule 5 is three-valued, not two: `latestHumanChangeAt` is a `Date`, `null` ("the shell checked and found none"), or `"unknown"` ("ordering could not be established"), and `"unknown"` refuses with `humanOrderingUnknown`. The first implementation collapsed the last two into `null`, so unavailable evidence read as "no conflict" and the write **applied** — the exact opposite of `manual-edits.md` §2 ("if reliable ordering evidence is unavailable, the safe default is to return a conflict and do nothing"). Not hypothetical: D9 records that timestamp reliability for the newer-fact rule is still open, so unknown ordering is an expected state. (`core/src/safety/write.ts`, `FINDING(safety-ordering-unknown)`.) | `supported` | The shell must distinguish the two when it computes the value; a shell that reports `null` for a failed lookup silently restores the old behaviour, so this is an executor-boundary test under D7 rather than a maintainer question. |
| D52 | The clock-triggered destructive gates are inescapable, and the kill switch is reported first on that path too. `evaluateWrite` refuses `clockTriggeredDestructive` outright (`wrongEntryPoint`); the general rules moved to an internal function that both entry points share. Found by audit, and it was the module's headline claim: `evaluateWrite` previously answered `apply` for a destructive request in an otherwise-permissive active context, so "a destructive action cannot fire without a recorded warning and an elapsed grace period" was enforced by which function the caller happened to call. Separately, the kill switch was reached only via the general rules at the END of `evaluateDestructive`, so an operator who had pulled the emergency brake was told `noWarning` — a refusal either way, but D39 freezes the verdict codes as contract and claims kill-switch-first. (`core/src/safety/destructive.ts`, `FINDING(safety-destructive-entry-point)`, `FINDING(safety-killswitch-order)`.) | `supported` | None for the mechanism. The exhaustive sweep now crosses action class, capability-link state, and the remaining context dimensions (5,120 combinations), asserting `apply` per combination rather than only checking an aggregate count; a destructive request can never apply from the general path. |
| D53 | `WriteContext` names the capability its enablement flag describes, and a mismatch against `request.capability` refuses (`capabilityMismatch`) before any policy check — a malformed pair is not a policy question. The request had always named a capability that no code read, while the context asserted a bare `capabilityEnabled` boolean about "the" capability, with nothing tying them together: a shell could answer the enablement question about a different capability entirely and rule 1 would pass. This converts an unnoticed attestation into a checked fact at no cost. (`core/src/safety/write.ts`, `FINDING(safety-capability-link)`.) | `replaced` | None. Reduces, and does not remove, the attestation debt named in `core/README.md`: `preconditionHolds` remains capability-specific and unverifiable in core. | **Replaced 2026-08-05 by D73's derivation:** `evaluateWrite` now takes the request and the reviewed configuration and reads the capability from `request.capability`, so a context describing a different capability cannot be constructed. The `capabilityMismatch` code, its check, and its tests are deleted — the state is unrepresentable rather than caught.
| D54 | The `immediatePreventive` action class has no dedicated explanation/reversal gate, so `evaluateWrite` refuses it with `preventiveGateUnavailable`. The class remains in the design because the approved-moderation issue-lock use case is real, but an unimplemented safety contract cannot inherit the weaker reversible-change path. (`core/src/safety/destructive.ts`.) | `supported` | The first capability that requests an immediate preventive action must specify and test what "explains immediately" and "simple maintainer reversal" require before the refusal can be replaced by an applying gate. |
| D55 | Label-mapping injectivity is judged case-insensitively and ignoring surrounding space, because that is how GitHub judges label-name uniqueness. Exact-string comparison accepted `status: ready` and `Status: Ready` as mappings for two different meanings — one label on GitHub, therefore one label carrying two meanings, which is precisely the ambiguity D34 exists to prevent and which the observation projection depends on being impossible. The configured spelling is preserved for writes; only the uniqueness key is folded. The old code deferred case to "the shell's concern", but the shell does not exist and D34's recommendation to reviewers claims full injectivity, so the implementation was weaker than the register said. (`core/src/config/validate.ts`, `FINDING(config-label-case)`.) | `supported` | Rides with D34's review: confirming full injectivity now also confirms the folding rule. If reviewers ratify the same-entity relaxation instead, the folding rule survives unchanged — it constrains sameness, not scope. |
| D56 | An absent `mode` defaults to `observe`; a present but empty one is a validation error. `raw.mode ?? "observe"` silently accepted `mode:` with no value — YAML parses that to null — and chose a mode on the maintainer's behalf. The mode it chose was the safe one, which is why this is minor, but silently interpreting malformed input is the pattern §2.7 and D38 reject everywhere else in the same file. (`core/src/config/validate.ts`, `FINDING(config-null-mode)`.) | `supported` | None. Consistent with D38: the maintainer sees an error and a configuration report rather than a mode they did not write. |
| D57 | An intent may require any permission its capability declares, repository- or organization-scoped. The check compared only against `permissions.repository`, so a grant legitimately declared under `organization` produced "which the capability does not declare" — a false failure that would have blocked any org-scoped capability, and `progression` needs organization-wide data by its own module document. (`core/src/capability/declaration.ts`, `FINDING(contract-intent-org-permissions)`.) | `supported` | None; this was a defect against `contract.md` §1 as written, not a policy choice. The permission *ceiling* remains stage-four policy and is unaffected. |
| D58 | The retirement tombstone is enforced in code, not just documented: `get` is the sole declaration lookup and returns `undefined` for retired capabilities, while `describe` exposes only report-safe name and retirement metadata. `activeNames` existed but nothing obliged a caller to consult it, and the old `get` handed back a retired declaration ready to run — so "a retired capability never activates" was a rule a caller had to remember. The reporting path can no longer yield an activatable declaration. (`core/src/capability/declaration.ts`, `FINDING(contract-retired-enforcement)`.) | `supported` | The stage-five effective-configuration report uses `describe` so it can say "enabled, but retired"; activation uses the only declaration lookup, `get`. |
| D59 | A `conflict` projection carries `blocked`, `closedBy`, and ignored cross-entity meanings alongside the conflicting positions. It previously lost context exactly when reporting needed it most. No write behavior changes: a conflict still produces no `WorkItemState` and cannot reach a write. (`core/src/workflow/project.ts`, `FINDING(observe-conflict-context)`.) | `supported` | None. Feeds the configuration and operator reports the D38 mitigations require. |
| D60 | A destructive warning is authority for exactly one request snapshot: action class, capability, dated cause, target item, and change. `createDestructiveWarning` copies those values into frozen primitive data, including numeric timestamps, so neither a shared target object nor a mutable `Date` can change the warning after issuance. It also records the earliest action time, cancellation action, and reversal. `evaluateDestructive` rejects cross-request reuse, warnings recorded before their observation, and earliest-action timestamps shorter than the full grace period. Without this binding, an old warning for one item authorized a new destructive request for another. (`core/src/safety/destructive.ts`, `FINDING(safety-warning-binding)`.) | `supported` | None for the mechanism. The durable warning store and executor must reconstruct warnings through the factory and preserve the primitive snapshot without weakening the comparison. |

| D61 | The capability vocabularies are CLOSED platform catalogues, not per-capability extensions: `core/src/capability/catalogue.ts` enumerates every observation, resolver, and intent operation, and a capability chooses from them. This answers contract.md §9's first open question ("select the first concrete observation, resolver, and intent types") and it was not a preference — per-capability intent shapes are unimplementable at the far end, because the adapter would need an executor for a type it has never seen. P3 falls out as a consequence rather than a rule to police: capabilities that share no vocabulary have nothing to call each other through. (`core/src/capability/catalogue.ts`.) | `hypothesis` | Stage four confirms the catalogue is the right closure point, and the first ratified capability proves the initial vocabulary is sufficient. A capability that needs an operation outside the catalogue extends the catalogue by review, which is the intended cost. |
| D62 | The platform owns an operation's idempotency class; the capability's declaration is a redundant statement that must MATCH. `capability/declaration.ts` accepted any class on any intent name, so a capability could declare `postManagedComment` as `idempotent` — and the executor trusts that class to pick its retry rule, so the declaration would route a lost response down the blind-retry path and reproduce experiment 6.5's demonstrated comment duplication, the exact failure the class exists to prevent. A per-capability field cannot be authoritative about a per-endpoint fact. `checkAgainstCatalogue` rejects the mismatch at registry build; `idempotencyOf` is what the planner reads. (`core/src/capability/catalogue.ts`, `FINDING(runtime-idempotency-declared-not-checked)`.) | `supported` | None for the mechanism — the failure mode is demonstrated and the check is tested. Stage four folds `checkAgainstCatalogue` into the platform's registry build so no shipped declaration can skip it. |
| D63 | `ActionClass` enters through the intent, with a per-operation FLOOR. safety.md's action classes had no supplier anywhere in the capability contract, so `evaluateWrite` — which requires one — could not be fed from an intent at all: the safety engine and the capability contract were written against each other and never met. It cannot be purely per-operation either, because `unassign` is a `reversibleStateChange` when a human asks and a `clockTriggeredDestructive` when a clock does, and only the capability knows which. The catalogue therefore names a minimum and the intent may declare stricter; laxer is refused. A capability may be more careful than the platform requires and never less. (`core/src/capability/catalogue.ts`, `FINDING(runtime-action-class-floor)`.) | `hypothesis` | Stage four confirms the floor values per operation. The floors encoded today are the weakest defensible reading, in the same spirit as `MIN_GRACE_DAYS`. |
| D64 | A destructive intent carries its own warning record (`DestructiveDetail`), and a non-destructive one carrying a warning is an ERROR. `evaluateDestructive` refuses without a warning, but contract.md §3's intent had no field for one, so a destructive intent could never have passed the gate it was designed for. The warning rides on the intent rather than being fetched by the planner because the capability is what decides an item is stale, so it is what must show its warning — a planner that looked the record up itself could pair a warning with an intent that was never about it. The reverse check matters more: a warning attached to a non-destructive intent reads as a grace period that no gate will ever consult, because `evaluateWrite` does not look at the field. (`core/src/capability/intent.ts`, `FINDING(runtime-destructive-intent-has-no-warning)`.) | `supported` | None for the mechanism. The first destructive capability confirms the four fields are sufficient; it is deferred well past the November milestone by the build plan. |
| D65 | `idempotencyKey` has exactly one derivation, in `deriveIdempotencyKey`: capability, repository, item, operation, and the CAUSE with its timestamp — deliberately not the desired payload, so a capability that recomputes a slightly different comment body for the same occasion does not create a second comment. contract.md §3 required the field and never said what produced it, while the store's primary key silently depended on the answer: two intents that collide are one already-done effect and the second is never performed, and the same intent keyed differently on redelivery duplicates. Batch-level collisions are refused in the planner, which is the only place a whole batch is visible — the store cannot see them, because from its side a collision is indistinguishable from a legitimate redelivery. (`core/src/capability/intent.ts`, `executor/src/planner.ts`, `FINDING(runtime-idempotency-key-underived)`, `FINDING(planner-key-collision)`.) | `hypothesis` | Stage four confirms the derivation, in particular that the cause timestamp is the right occasion boundary. A capability whose cause is not event-shaped (a sweep over items with no per-item event) would need a different occasion key, and `inactivity` is the probe that will test it first. |
| D66 | One intent produces one plan. The executor claims per `effectId`, so grouping is load-bearing and contract.md never chose. Separate plans, because a shared one couples unrelated outcomes three ways: a crash mid-plan blocks siblings behind a claim they have no relationship to; D45's revision guard invalidates the whole plan on a configuration change, discarding intents that were still valid; and the journal's contiguous `seq` imposes an order the capability never asked for. Grouping can be added later if a capability needs atomic ordering; ungrouping a shared plan after the fact cannot. (`executor/src/planner.ts`.) | `hypothesis` | A capability that genuinely needs two effects to land together, or not at all, would reopen this. None of the candidate capabilities does. |
| D67 | The repository mode is authoritative from the REVIEWED configuration, and a planner whose rechecked `WriteContext.mode` disagrees refuses (`modeMismatch`) rather than picking a winner. The mode existed in two places — `RepositoryConfig.mode` and the `WriteContext` that `evaluateWrite` actually reads — with nothing requiring agreement, so a shell that assembled the context from anywhere else would write in `active` on a repository whose reviewed file said `dry-run`, and every test would still pass because each half is self-consistent. This was not reasoned out in advance: the first composition test set the configuration to `dry-run`, received a plan, and exposed it. Goal 2 makes the configured value authoritative, so a disagreement is a shell defect and must surface; silently overriding the context would repair the symptom and leave the defect unobserved. (`executor/src/planner.ts`, `FINDING(planner-mode-duplicated)`.) | `replaced` | None for the guard. Stage five's shell must source the context's mode from `parseConfig` output and nowhere else; this row is the reason that is a requirement rather than a convention. | **Replaced 2026-08-05 by D73's derivation:** the planner no longer carries a mode to compare, because the safety engine reads mode from the configuration directly. `modeMismatch` and its test are deleted for the same reason as D53's.
| D68 | Dry-run and observe stop at PLANNING, before a plan exists — not at the port. An unapplied plan would still be journalled, and the recovery loop would later find an open intent it is obliged to resolve against GitHub for a write that was never meant to happen: manufactured operator work out of a mode whose whole promise is that nothing happens. Safety rule 10 is therefore a planning decision, not an execution one. (`executor/src/planner.ts`.) | `supported` | None. The behaviour is tested end to end against the real store — a dry-run batch leaves `openIntents` empty. |
| D69 | The single declared idempotency class per intent holds only while every catalogue operation is SINGLE-CALL. `IntentDeclaration` carries one class; `PlannedCall` carries one per call; and contract.md §5 permits multi-call plans, which mix them (create-comment then add-label is non-idempotent then idempotent). Every operation is one call today, so the constraint is recorded rather than repaired. The first multi-call operation must move the class from the intent declaration onto the call, or plans will retry under the wrong rule. (`executor/src/planner.ts`, `FINDING(planner-per-call-idempotency)`.) | `hypothesis` | Whichever capability stage four ratifies: if any of its effects needs more than one call, this row must be resolved before the effect is built. |
| D70 | **P3 is now tested in code.** `design/build-plan.md` §12 recorded P3 as "a supported principle on paper and an untested claim in code" and deferred the toggle matrix past November on the grounds that one capability cannot violate it. That is right about the arithmetic and wrong about the prerequisite: P3 is a STRUCTURAL property, so three disposable stubs test it exactly as well as three shipped capabilities would. The run exists (`probes/test/toggle-matrix.test.ts`): all eight subsets of three deliberately dissimilar capabilities, asserting that each one's intents, explanations, and resolver calls are identical regardless of which neighbours are enabled, that a disabled capability is never evaluated, and — as a negative control against a vacuous pass — that each capability alone still produces work. The isolation is structural rather than asserted: a capability is reachable only through its own projected view. | `hypothesis` | The result transfers to shipped capabilities only insofar as they use the same boundary, which is the point of putting the boundary in `core`. Stage four should confirm that this discharges §12's post-November commitment; the matrix should be re-run with the first real capability substituted for its probe. |
| D71 | A capability sees which meanings are MAPPED, never what the repository calls them. contract.md §6 says a capability "refers to internal meanings rather than repository label strings", and passing `mappings.labels` into the capability view would have satisfied every type while quietly breaking that rule. `CapabilityView` therefore reports availability only, and omits three further things a capability must not decide: the repository mode (rule 10's job), its own enabled flag (it is never evaluated while off), and any other capability's block. A capability that never sees a label string cannot hard-code one. (`core/src/capability/boundary.ts`.) | `supported` | None for the projection. Whether `mappedMeanings` is enough for a capability that wants to explain itself in the repository's own words is a stage-four question — the managed comment is rendered by the platform, which does have the mapping. |
| D72 | A `DestructiveWarning` is rebuilt at act time from the STORED warned cause, never from the current request. D60 makes the warning a branded object only `createDestructiveWarning` can construct — a real within-process guarantee, and one that cannot survive the journey a warning actually makes: it is issued in one process run and acted on days later across restarts, so it must be persisted as plain data and rebuilt, and the brand is a runtime symbol that does not serialize. Rebuilding it from the current request satisfies every type and makes `warningMatchesRequest` compare a value with itself — a safety check that can never fail, which is worse than none. `DestructiveDetail` therefore carries the warned cause and its observation time, and the planner rebuilds from those. The seam narrows D60's comparison to the causal fields: item and change derive from the same intent at both ends and cannot diverge, while the cause is the one part a capability can get wrong, and getting it wrong is exactly the reuse D60 forbids. Found by merging this branch onto D60 rather than by either change alone. (`core/src/capability/intent.ts`, `executor/src/planner.ts`, `FINDING(runtime-warning-cannot-cross-the-store)`.) | `hypothesis` | Stage four confirms that plain-data persistence plus rebuild is the intended reading of D60 rather than a weakening of it, and stage five's store design decides which columns hold the warned snapshot. |
| D73 | `WriteContext.capabilityEnabled` DUPLICATES `CapabilityConfig.enabled` with nothing comparing them, and is the last unguarded member of a pattern the register has now hit four times: a fact stored in two layers, free to disagree, closed by adding a comparison. D53 added `capabilityMismatch` for the capability NAME and D62 added a catalogue check for the idempotency CLASS, but consent itself — the boolean that decides whether a repository agreed to this capability at all — is still a bare attestation. A shell can pass `capabilityEnabled: true` for a capability the reviewed configuration disables; D53's name check passes, because the names match, and goal 2's "no configuration means no workflow-changing writes" is defeated by a field nobody compares. Found by reading the four duplicates together rather than by any one of them failing. The recommended repair is NOT a fifth guard: `WriteContext` should be DERIVED from the parsed configuration and the declaration the caller already holds, which retires D53's and D67's guards as well and makes this state unrepresentable rather than merely checked. (`core/src/safety/write.ts`, `core/src/config/schema.ts`.) | `hypothesis` | Stage four decides between the derivation and a fifth comparison. The derivation is the recommendation and is proposed as its own packet item; until it lands, the shell must source `capabilityEnabled` from `parseConfig` output and nowhere else — the same standing requirement D67 places on `mode`. |
| D74 | One internal representation for an instant: epoch MILLISECONDS, converted only at the two real edges — GitHub in, SQLite out. A single logical moment currently crosses three encodings between capability and journal: `Date` through most of core (`WriteRequest.causeObservedAt`, `DatedCause.observedAt`, every observation payload), `number` inside the destructive warning (`warnedAtMs`, `earliestActionAtMs` — D60 chose primitives to avoid aliasing a mutable `Date`), and canonical ISO strings in the store, which validates the exact `Date.toISOString()` shape defensively because it cannot trust its caller. The executor takes `now: () => string` while the planner beside it takes `now: Date`. Each choice was locally right — `Date` is ergonomic, milliseconds are immutable, ISO sorts lexicographically in SQLite — and the aggregate is a conversion at every boundary plus a validator that exists only because the boundaries disagree. Milliseconds are the honest internal form: immutable like D60 requires, comparable without parsing, and needing no format check. ISO stays where it earns its keep, in the store's sort order. | `hypothesis` | Stage four accepts or rejects the single representation. If accepted, the store's timestamp validator is deleted rather than ported, and D60's aliasing rationale is satisfied by the type rather than by convention. Cost is honest: most test fixtures construct both forms today. |
| D75 | One result convention, and machine-readable codes on configuration errors. Core currently discriminates failure three ways — `ok` in `config/`, `capability/declaration.ts` and `capability/intent.ts`; `outcome` in `safety/` and the executor; `kind` in `workflow/project.ts` and the planner — and carries two incompatible error payloads: bare `readonly string[]` from `parseConfig` and `validateDeclaration`, versus `code` plus `reason` everywhere else. The discriminant spread is ordinary inconsistency; the payload split has a concrete consequence. D38's fail-closed granularity is acceptable only because of two named mitigations — the effective-configuration report and PR-time validation as a check run — and both are downstream consumers that can only ECHO prose, because configuration errors carry no code to group, count, link, or localise by, while every refusal elsewhere in core does. The mitigation that makes D38 humane is weaker than it needs to be, for a reason that is purely a type choice. Landed 2026-08-05 as `ConfigError { code, message, path }` — eleven codes over fifteen error sites, each carrying the dotted path a check run needs to annotate a line (`FINDING(config-error-codes)`, `core/src/config/schema.ts`, `core/src/config/validate.ts`). The discriminant half of this row — collapsing `ok`/`outcome`/`kind` into one word — was deliberately NOT done: it churns every consumer for a consistency nobody is blocked on, where the payload split had a blocked consumer in D38's configuration report. | `hypothesis` | Stage four picks one discriminant and rules that every failure carries a `code`. The prose stays prose — `safety.test.ts`'s convention of asserting `reason.length > 0` and never the wording is the right one and should extend rather than change. Mostly mechanical; no behaviour moves. |
| D76 | Every exported enumeration is declared ONCE and its union derived from it — `export type X = (typeof XS)[number]` — and a set keyed by a union is written as a mapped type, so adding a member breaks compilation until every consumer is updated. `REPOSITORY_MODES` lived in the configuration module while a hand-written `RepositoryMode` union lived in the safety module: the same four strings in two files with nothing linking them, and a `value as RepositoryMode` cast in the parser quietly covering the seam. Adding a mode to the array alone would have let `parseConfig` accept a value the safety engine's type had never heard of. `PROFILE_EDGES` had the same shape more mildly, listing `issue` and `pullRequest` literally instead of mapping over `EntityKind`. Fixing the first also removed the type-only import cycle D73 introduced, because a repository mode belongs to configuration and nothing in `config/schema.ts` now imports from `safety/write.ts`. This is the FIFTH sighting of one fact stored in two places — after D53, D62, D67 and D73 — so the repair is not another comparison but a test: `checks/test/repo-artifacts.test.ts` now fails on any exported const array in `core/src` with no union derived from it, with a negative control proving the detector can fail. Found by reading, which does not scale; the next one will be found by the suite. (`core/src/config/schema.ts`, `core/src/safety/write.ts`, `core/src/workflow/transitions.ts`, `FINDING(config-mode-union-derived)`.) | `supported` | None for the mechanism — the invariant is enforced and controlled. The pattern's recurrence is the thing worth a decision: five instances in one package suggests it should be named in `design/testing/README.md` as a class to check for, alongside the tier definitions. |
| D77 | The shell supplies OBSERVATIONS; core computes every CONCLUSION. `WriteContext` carried five facts, two of which core already knew: `itemBlocked` duplicated what `project.ts` computes from the same observed meanings, and `installationHasPermission` was a boolean summary of a comparison core could make itself. Both are now derived — `isBlocked` is defined once in `workflow/meanings.ts` and used by the projection and the safety engine alike, and `missingPermissions` compares an intent's requirements against the grant the shell reports. The derivation is not only safer, it is more useful: a refusal now names the missing grant, where a boolean could only ever say that one was missing. `RepositoryConfig` also gains its own `revision` — the executor guards every in-flight effect on that string and D45 rules old-revision intents unresumable, yet the parsed configuration carried no identity, so two configurations could share a revision with nothing able to notice. `PermissionGrant` moved to `core/src/github/permissions.ts`, both because `safety/` needed it without importing `capability/` and because a `scope:level` string is a GitHub fact. This is the eighth and last sighting of one fact stored twice — after D53, D62, D67, D73, D76 and the two closed here — and the first closed by REMOVING the second copy rather than comparing it. (`FINDING(config-revision-detached)`, `FINDING(safety-context-derived)`; `core/src/safety/types.ts`, `core/src/safety/rules.ts`, `core/src/workflow/meanings.ts`, `core/src/github/permissions.ts`, `core/src/config/schema.ts`.) | `supported` | One framing question for stage four: `RepositoryConfig` now carries a field no maintainer writes, so a parsed configuration is no longer a valid document — the round-trip property test had to strip the stamp. The alternative is a wrapper (`{ revision, document }`), which is purer but adds indirection at every `config.mode` read and reintroduces two things that can drift. The stamp was chosen; the wrapper is the named alternative. |
| D78 | The workflow transition tables are RULES, and `screenIntent` enforces them. D29 already said capabilities move along documented edges while humans may land anywhere; nothing enforced the first half. The tables were exhaustively tested, checked against the design document by `doc-drift.test.ts`, and corrected three times by D48's audit — with ZERO callers. A capability could put `readyToMerge`, a pull-request meaning, on an issue, and both the intent screen and the safety engine passed it, because safety asks about permission, mode, blocking and human conflict, never about workflow legality. The missing link was one field: `Intent.cause` was `string` while `ISSUE_CAUSES` and `PR_CAUSES` had been closed, entity-scoped unions all along. Only the MOVING operation gets a typed cause — `applyMappedLabel` carries a `TransitionCause`; a comment and an unassign have reasons but not transitions, and keep the free-text dated cause that identifies the occasion. The check is self-contained in `screenIntent`: entity kind, the claimed `from` (the same `expected` safety rechecks as `preconditionHolds`), the desired `to`, and the cause. Two new screen codes: `meaningWrongEntity` and `transitionNotOnMap`. (`core/src/capability/intent.ts`, `core/src/capability/catalogue.ts`.) | `hypothesis` | The review should confirm the tables are rules rather than a profile — the alternative, and the one P7 and the build plan's "optional Hiero workflow profile" language point at, is that they are one repository family's documentation and belong outside `core/`. The type change immediately proved its own worth: `intake` stopped compiling until someone named its cause from the map, and the answer was `intakeObserved`. Whether every capability can express its reason in the closed set is now a compile-time question rather than one somebody has to remember to ask. |
| D79 | Only a human may pause an item: `screenIntent` refuses a capability that applies the mapped `blocked` meaning, with its own code `pauseNotCapabilityWritable`. Pausing is the ONE write whose blast radius is other capabilities — `itemBlocked` is not a rule about the item, it is a rule about every capability's access to it — so a capability able to set it would hold a veto over the others, acquired through shared state and without calling them. That is the coupling P3 forbids in its least visible form: the vetoed capability sees `itemBlocked` and has no way to learn who caused it. It is also the consistent reading of D54, which already refuses `immediatePreventive` outright pending an explanation-and-reversal gate: "detect a problem, freeze the item" is exactly that class, and letting a capability reach the same outcome by writing a label would be a hole in D54. The refusal is deliberately NOT `meaningWrongEntity` — `blocked` is legal on both entity kinds, and telling a maintainer they got the entity wrong when they actually tried to withhold an item from every capability would be a false reason; a test pins the rule order so the two cannot be swapped silently. Nothing about pausing changes, only who may do it. (`core/src/capability/intent.ts`.) | `hypothesis` | Stage four confirms that capabilities never pause. The named alternative is to allow it for a capability that can show an immediate explanation and a simple reversal — which is D54's gate, so the answer is to build that gate rather than to reopen the label path. A capability that genuinely needs to stop work is the evidence that would reopen this. |
| D80 | `removeMappedLabel` is deleted from the operation catalogue, and `applyMappedLabel` means SET the position rather than add a label — the adapter removes the position label the item previously held as part of realising it, which is D4's rule that the platform removes only named managed labels. The operation had no legitimate use left once D78 and D79 landed, and no capability ever declared it. Removing a POSITION is not on the map: every `to: null` edge is a closure, and D47 makes closure a reason read from GitHub, never a label the platform writes — so leaving a position without closing is not a documented move, and a position CHANGE is an apply. Removing `blocked` is unpausing, which needs the same authority as pausing and D79 reserves to humans. Removing a label a human misapplied is exactly the automatic repair D29 and D35 refuse. An operation with no legal use is dead vocabulary in a closed catalogue (D61) — the kind that gets used later without anyone rechecking whether it was ever allowed, which is how the transition tables went unenforced for months. Deleting the name is safe in a way deleting a CAPABILITY name is not (D58): operations never appear in a repository's configuration, so no reviewed file can fail closed over this. (`core/src/capability/catalogue.ts`, `executor/src/planner.ts`.) | `hypothesis` | The evidence that would reopen it is a capability that must take a position OFF an item without closing it or replacing it — at which point the answer is a documented edge to `null` with a non-closure cause, not an unscreened operation. The ambiguity that made this operation look necessary was `applyMappedLabel` reading as "add a label"; that is now stated as "set the position" in the catalogue. |
| D81 | An observation carries the PROJECTION, not a flattening of it. `workflow/project.ts` distinguishes a CONFLICT — more than one own-flow position, which D35 forbids repairing — from a position carrying its meaning, blocked flag, closure reason and the ignored cross-flow meanings, and it is tested exhaustively over all 128 meaning subsets. The observation payload discarded that: it carried a bare `meanings` list plus separate `blocked`, `closed` and `merged` booleans, so the conflict signal died at the boundary and a capability handed an issue a human had put in two positions could not tell it from a clean one. The payload's own comment said "`null` when it conflicted" — behaviour the type never had, which is how long the gap had been there. `blocked` was also a DUPLICATE the shell had to keep consistent with the same meanings it derived from, the ninth sighting of one fact stored twice after D53, D62, D67, D73, D76 and D77; `closed` and `merged` were likewise restatements of `state.closedBy`. Carrying `ObservationProjection` removes all four. Capabilities now handle three outcomes rather than two — conflict, position, nothing — which is correct, because the situation has three cases and the third was silently collapsed into the second. The type change proved it immediately: both probes had to be given conflict handling they did not have, and `intake` would have triaged a conflicted issue. (`core/src/capability/catalogue.ts`, `probes/src/`.) | `hypothesis` | One question follows: `Intent.expected.meaningsPresent` is now a restatement of something the capability was just handed, which is the same duplication in a new place. The screen's `positionConflict` check (D78) reads that claim rather than the projection, so a capability that copies one of two positions still validates the wrong edge. Closing it means the screen reading the observation, which the intent does not currently carry. |
| D82 | A documented example is only documentation until something runs it. `design/config/schema.md` §3 showed two YAML configurations and no code had ever read YAML — `parseConfig` takes `unknown`, and every caller so far handed it an object literal. Both examples were checked and both parsed, by nobody's doing: the on-disk format was unspecified, so a document could fail in ways the error catalogue had no word for, and the only signal would have been a maintainer's confusion. Two artifacts now close this, and the split between them was learned the expensive way. `examples/config/` holds the four VALID documents — the adoption ladder, with the observe→active step asserted to change the mode and not the vocabulary — read by `checks/test/examples.test.ts` through the entry point the shell will use. The REJECTIONS began there too, as one file per `ConfigErrorCode`, and scored the new module at 0.00% mutation: Stryker's sandbox contains `core/` and nothing above it, so root-level fixtures ran under vitest, passed, and could not kill a mutant — the third silent-skip of this shape, after the `src/*.ts` mutate glob and the capability boundary being covered only from `probes/`. They are now an in-package corpus (`core/test/config/documents.ts`, thirty documents, several shapes per code), exhaustive by a mapped type over the union so adding a code fails compilation until a document reaches it (D76's rule applied to a catalogue). Three findings from writing it: `duplicateKey` earned its own code as the only malformed document that SUCCEEDS — YAML keeps the last value, so `mode: observe` followed by `mode: active` parses cleanly into a repository that writes; the alias limit THROWS rather than returning, so a hostile file in a pull request would have crashed the shell instead of producing a finding — converted, keeping the property that every rejection is a value, with the budget pinned at ten by a twenty-alias document the library's own default would accept; and a first draft appended `linePos` to messages that already carry position and excerpt — the TENTH one-fact-twice after D53, D62, D67, D73, D76, D77 and D81, caught because mutation flagged the guard as untestable, and fixed by deleting the duplication rather than testing it. Core gains its first runtime dependency (`yaml`, zero transitive). (`core/src/config/document.ts`, `core/src/config/schema.ts`, `core/test/config/documents.ts`, `examples/config/`.) | `hypothesis` | Two. The dependency is the reviewable half: the alternative is YAML living in the shell, which keeps core clean and leaves syntax errors outside the catalogue that every other configuration problem is in. And `ConfigError.path` is a dotted path into a mapping, so a file that never became a mapping has none — position lives in the library's message. A structured `position` field is the named alternative, deferred because it widens a type every consumer reads for one producer's benefit; the PR-time check run is the consumer that will decide it. |
| D83 | `docs/` exists for maintainers who will not read much: three pages, config first, written in the confident register of a shipped product rather than the cautious one of a prototype — the mode ladder is offered as a choice, not prescribed as a rollout. `configuration.md` is a per-key definition list (type, required, default, allowed) after CodeRabbit's shape, plus the CONTEXT the tables cannot carry: why label mappings exist at all, and that the required set depends on which capabilities you enable. Every table restates a closed vocabulary the code owns — modes, meanings, the error catalogue, the refusal codes — which is one fact in two places, aimed at a reader who cannot run the compiler that would catch the drift. So the tables are locked instead of trusted: `checks/test/docs.test.ts` parses each table and requires set equality with the union (mapped types where no runtime array exists, D76's rule). `TOP_LEVEL_KEYS` moved to `schema.ts` and is exported so the definition list can be held to it in both directions — a reference guide's characteristic failure is not being wrong but being INCOMPLETE, and a key nobody documented is discovered by a maintainer when their file is rejected, and the grouping in `troubleshooting.md` — "on purpose" versus "needs you" — is asserted against `report/convert.ts`'s severity classification, so the docs cannot tell a maintainer to relax about something the platform classifies as a problem. Boundary rule: `docs/` never cites a D-number or a source file; when a page needs a why, it links to `design/`. One deliberate exception: `docs/to-do.md` is maintainer-facing by design — it records what the user-facing pages cannot yet say truthfully and cites the register rows that unblock them, so the docs' gaps are as auditable as their claims. It is not linked from the user index. (`docs/quickstart.md`, `docs/configuration.md`, `docs/troubleshooting.md`, `checks/test/docs.test.ts`, with `docs/README.md` a three-link index because GitHub renders it on directory navigation.) | `hypothesis` | The pages state what the built logic does, but none of it has run against a real repository, and `docs/README.md` says so in its first line. The claim to revisit at stage four is the audience assumption itself — that config-first, table-first is what SDK maintainers want. The interviews are the test. |
| D84 | A capability must declare the mapped meanings it needs. Writing the user-facing configuration guide asked a question the platform cannot answer: which labels must a maintainer map to use a given capability? `CapabilityDeclaration` states triggers, config keys, observations, resolvers, intents, permissions and operational needs — not meanings. So the requirement is discovered at RUNTIME, when the capability finds its meaning unmapped and skips itself with an explanation, and the maintainer's experience of enabling `intake` against an unmapped repository is silence plus a buried report line. With meanings declared, `parseConfig` gains the check it obviously wants — an enabled capability whose meanings are unmapped is a configuration error with a path, caught by the PR-time check run before anything runs — and `docs/` gains a per-capability requirement table that can be locked like every other list. The gap is documented honestly in `docs/configuration.md` rather than papered over. Related: `design/config/schema.md` §227 still leaves the reviewed file's path unchosen, and a confident quickstart cannot have a blank there; `.github/hiero-automations.yml` is used throughout `docs/` as the working assumption. | `hypothesis` | Two decisions for stage four, both small and both blocking a claim the docs already make. The declaration change adds one field, one validator rule and one error code. The path needs choosing, not designing — the docs assume `.github/hiero-automations.yml` and every example will need a rename if that is wrong. |
| D85 | Tests about the REPOSITORY live in their own workspace package, `checks/`, not inside core. Four files in `core/test/` read outside the package: `docs.test.ts` (reads `docs/`), `examples.test.ts` (reads `examples/config/`), `doc-drift.test.ts` (reads `design/`), and `repo-artifacts.test.ts`, which reads the whole repository root and imported nothing from core's `src/` at all. The costs were not hypothetical: core's suite failed when a markdown file changed; the rejection fixtures scored `document.ts` at 0.00% because Stryker's sandbox is `core/` and root-level files are not in it (D82); and core source had begun to justify an export by naming `docs/`. The boundary now points one way — `checks/` depends on core through its public barrel (all nine imports were already public; none needed opening), and core does not know the repository exists. `checks/` has no `src/`, which is the point: nothing in it can or should kill a mutant, so nobody will again mistake a repository check for a package test. Verified during the move: `transitions.ts` holds 95.56% mutation without `doc-drift.test.ts` in the package, so the relocation cost core no coverage, and CI's `pnpm -r test` picks the package up with no workflow edit. The citation invariant caught all six stale paths the move created, including in its own D-rows. | `supported` | The inclusion rule for the future: a test that reads another package's files, or the repository root, goes in `checks/`; a test that can kill a mutant in `core/src` stays in `core/test`. The named risk is drift toward a junk drawer — `checks/` accepting behaviour tests because it is easy; the no-`src/` shape is the guard, and the trigger to revisit is the first test there that imports more than core's barrel. |
| D86 | The top level holds workspace packages and exactly three knowledge roots — `design/` (internal why), `docs/` (users), `examples/` (users, executable) — and nothing else. `audit/` and `planning/` predated any such rule: the first was fieldwork evidence frozen since 2026-07-17 with nothing marking it frozen, the second was four roadmap files whose genre already had a second home (the ratification packet sat in `design/` while the stage-four packet sat in `planning/`). Both moved under `design/`, each gaining the README it never had — `design/audit/README.md` declares the fieldwork read-only and rules that corrections are new decision rows, not edits to evidence; `design/planning/README.md` separates living from historical, which is the whole treatment of legacy documentation: never deleted, always labelled. `experiments/` needed nothing — untracked by design (D32), its conclusions already migrated. The move exposed a blind spot worth more than the move: sixteen documents cited `audit/…` and `planning/…` paths and the citation invariant stayed green, because it validated only TypeScript paths — and its package alternation did not know `checks/` existed either. Both fixed: repo-rooted document paths (`design|docs|examples/….md|.yml`) are now checked with the same rigour as source paths, which is only possible BECAUSE the top level is closed — an open top level has no alternation to write. A new invariant enforces the sentence itself, with `audit`/`planning` as the negative control. (`checks/test/repo-artifacts.test.ts`, `design/audit/README.md`, `design/planning/README.md`.) | `supported` | The named cost: `design/` grows heavier, and if a future document class is neither internal rationale nor user-facing it will strain the three-root rule — the first candidate to watch is operator-facing runbooks when the App ships, which may deserve a fourth root, added deliberately rather than by drift. |

### Adoption record — 2026-07-25

Sophie (exploreriii), as project architect, adopted the following rows as **working architecture**: strong
enough to guide the next work (`supported`), with formal `ratified` status still requiring the stage-four
review per §5. Adoption fixes the working values; stage four may revise them.

- **Storage and recovery**: the storage decision's closures (D1, D13, D24, D27, Q15) stand as working
  architecture; formal close-out happens at stage four. D41 adopted with a **15-minute lease**; D42
  adopted; D43 adopted with **requeue threshold = 2× lease** and **90-day retention** for `seen_delivery`
  and done journal rows; D44 adopted at **5 attempts**; D45 adopted as **operator action** for the first
  version; D46's precondition accepted, with the staleness measurement **gating `active` mode**.
- **Safety and taxonomy mechanics**: D29 and D33 adopted as encoded.
- **Operations**: D40 adopted with a **quarterly** re-probe cadence; the fixture-refresh owner falls out
  of Q13.

Not adopted — deliberately left `hypothesis` for the stage-two maintainer conversations and the named
reviews: D28, D30, D31, D34, D35, D38, D39 (including D39's security-control sub-question for D22). The
working defaults these rows encode remain in force until those reviews answer;
[`ratification-packet.md`](ratification-packet.md) §7 carries the questions.

**Subsequent evidence (2026-07-28):** the PR #25 risk review reopened D41. The 15-minute working value is
not an exactly-once proof; live lease takeover remains merge-blocking until the overlap contract and oracle
named in the row exist. The other adoption entries are unchanged.

**Rows added after this record.** D47–D50 (2026-07-29) postdate the adoption above and are **not** covered
by it. D50 is mechanical and needs no review; D47, D48, and D49 join the workflow-profile agenda and the
stage-two questions in [`ratification-packet.md`](ratification-packet.md) §3 and §7. They arrived from a
different source than D28–D46 — not from coding ambiguous prose, but from reading the finished tables back
against `design/audit/`, which found an edge missing from the design document itself. That suggests the audit is
worth a second pass against every table the implementation now encodes, not only this one.

**Audit rows — 2026-07-30.** D51–D59 came from a deliberate adversarial read of `core/`, and their source is
worth recording because it differs again: not ambiguous prose (D28–D46) and not the audit of existing Hiero
automation (D47–D50), but asking of each module "what does this claim, and can I make it false?". Four of the
nine were live defects rather than unstated choices — D52 (a destructive request could be applied through the
general write path), D51 (unknown ordering read as no conflict), D57 (org-scoped intents falsely rejected),
D58 (the retirement tombstone unenforced). D52 is the one to learn from: it survived 152 passing tests,
including a sweep advertised as exhaustive, because the sweep was exhaustive in seven of eight input
dimensions and the omitted dimension was exactly where the defect lived. The lesson for
`design/testing/README.md` tier 2 is that "exhaustive" must name the dimensions it enumerates.

**Rows added after this record (2026-08-05).** D61–D73 come from a fourth source: building the seam BETWEEN
the finished packages rather than auditing any one of them. `core/`, `store/`, and `executor/` were each
verified against their own source documents, and the only coupling between them was a branded id and a type
import — nothing had ever run a capability decision through to an effect. Writing the runtime boundary
(`core/src/capability/`), the intent-to-plan translator (`executor/src/planner.ts`), and three disposable
probe capabilities (`probes/`) surfaced eight gaps that no further work inside a single package would have
found, because each package was individually correct. D73 is the clearest of them: four separate rows now
close the same shape of defect — a fact stored twice, free to disagree — and reading them together showed the
fourth instance had never been closed at all. Two of them (D62, D64) are cases where the safety
engine and the capability contract had been written against each other and did not meet. One (D67) was
found by a failing test rather than by reasoning, and is recorded that way deliberately. The probe
capabilities are throwaway — see [`probes/README.md`](../probes/README.md) — but these rows are not.

D72 deserves its own note, because neither change produced it alone: the 2026-07-30 audit made destructive
warnings branded and immutable (D60), this branch made a destructive intent carry its own warning (D64), and
only merging the two exposed that a branded object cannot cross the store a warning must actually cross.
Two individually correct changes, one gap between them — the same shape as the rest of this block, one level
up. It is worth expecting more of these wherever an audit and a seam meet.

None of D61–D73 depends on the stage-two ranking, on D32's precedent: capability *choice* waits for
maintainer data, but the capability *boundary* names no capability. They are collated for review in
[`ratification-packet.md`](ratification-packet.md) §2b — the stage-four architecture session, not the
workflow-profile one. Only D69 reaches into the first-capability choice, as a gate recorded in
[`planning/stage-four-review-packet.md`](planning/stage-four-review-packet.md) §7.

**D74 and D75 (2026-08-05) are a different kind of row again.** They are not defects and not boundary
decisions: they are the two cross-cutting inconsistencies that a read of the whole package surfaced, where
every individual choice is locally defensible and the aggregate costs a conversion or an adapter at every
seam. They are proposed together with D73's recommended derivation in
[`ratification-packet.md`](ratification-packet.md) §2c, because all three are the same request: let stage
four decide the simplification BEFORE the code is written, rather than ratifying guards that a later
refactor would retire.

## 4. Open product and engineering questions

| Identifier | Question | Decision owner or evidence | What the answer blocks |
|---|---|---|---|
| Q1 | Which organization hosts and operates the GitHub App? | The Hiero or LFDT infrastructure owner must decide. | Production deployment is blocked. |
| Q2 | Which maintainer problems become the first user-facing capabilities? | Maintainer demand and the capability review must decide. | The user-facing minimum viable product is blocked. |
| Q3 | Do any repositories want the optional skill ladder, and what counts as a completion? | The affected repository maintainers must decide. | Skill-based assignment and progression are blocked. |
| Q4 | Which human actions may automation ever refuse or reverse? | Maintainers must review the selected capability policies. | Manual-edit enforcement is blocked. |
| Q5 | How does assignment behave with native assignment and multiple assignees? | The assignment specification and sandbox scenarios must decide. | The assignment capability is blocked. |
| Q6 | Is review queue state stored, derived, or left to native GitHub review rules? | Repositories that request review routing must decide. | Review routing is blocked. |
| Q7 | How are old labels, configuration, and writers migrated without two systems writing the same state? | A per-repository migration plan must decide. | Existing repository cutover is blocked. |
| Q8 | Which repository volunteers for a read-only or reversible pilot? | Repository maintainers and the operator must decide. | Ring-one testing is blocked. |
| Q9 | What is the managed-comment marker and schema migration format? | The managed-comment implementation and sandbox test must decide. | Stable comment recovery is blocked. |
| Q10 | What rate and timeline-read budget is safe for the fleet? | Answered by experiment 6.4 (2026-07-23): every call costs one unit of the 5,000-per-hour installation budget, conditional 304 re-reads are free, and content creation hits an unsignaled secondary limit near eighty writes per minute — write pacing must stay well below that, serialized per GitHub's guidance. A sweep of fifty repositories with twenty active issues each costs about one fifth of an hour's budget uncached; with conditional reads, steady-state cost tracks changed issues only. | Unblocked: sweep cadence can be designed against these numbers. |
| Q11 | Is the App installed only on selected repositories or more broadly with repository configuration as a second gate? | Organization governance must decide. | Installation guidance is blocked. |
| Q12 | Which implementation documents should be written alongside working code? | The first implementation work packet must decide. | This question does not block feasibility work. |
| Q13 | Who owns architecture review, the development App, the platform components, testing, and rollout? | Sophie and the project maintainers must name owners. | Committed dates and production work are blocked. |
| Q14 | What path, strict YAML schema, and schema migration policy should the App use? | The configuration design review must decide the final path and migration policy. Experiment 6.3 (2026-07-23) validated the mechanics against `.github/hiero-automation.yml`: strict parsing, unknown-key rejection, and `schemaVersion` rejection all fail closed, and a default-branch config change is event-observable within seconds. Inheritance is deferred from the first version. | The configuration implementation is blocked only on the path and migration decisions. |
| Q15 | What minimum owned operational storage is required, and which technology should provide it? | Answered by experiment 6.5 (2026-07-23): a single-file SQLite store with four small tables — seen delivery ids, effect intent/done journal, claims, schedules. GitHub keeps all effect outcomes and resolves every sent-but-unconfirmed write. See `design/operations/storage-decision.md`; ratification pending per §5. | Unblocked: production recovery and hosting design can proceed from the storage decision. |
| Q16 | What exact narrow operations and typed results belong in the first adapter? | Answered by the endpoint and permission matrix (`design/operations/endpoint-permission-matrix.md`, completed 2026-07-23): every operation the first capabilities need is confirmed with its permission, quota cost, conditional-read support, and failure shape; the matrix rows are the first adapter's operation list, and the failure catalogue fixes the error types it must expose. | Unblocked: the adapter implementation can be specified from the matrix. |

## 5. Ratification rule

A hypothesis becomes `ratified` only after the register names the approving maintainers, the date, and the
evidence they reviewed. A sandbox result may reject a hypothesis without selecting the final replacement.
When that happens, the row becomes `reopened` until a later decision is ready.

[`ratification-packet.md`](ratification-packet.md) collates every currently pending row into per-venue
agendas with recommended answers — the stage-four working document. Outcomes land back here, not there.
